name: Merge RSS Feeds

on:
  schedule:
    - cron: '0 */6 * * *'    # кожныя 6 гадзін
  workflow_dispatch:         # Ручны запуск

permissions:
  contents: write

jobs:
  merge-rss:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: pip install feedparser requests python-dateutil

      - name: Run RSS merger
        env:
          RSS_FEEDS: ${{ secrets.RSS_FEEDS }}
          PRIORITY_TOPICS: ${{ vars.PRIORITY_TOPICS }}
          BLACKLIST_TOPICS: ${{ vars.BLACKLIST_TOPICS }}
          OUTPUT_TITLE: ${{ vars.OUTPUT_TITLE || 'Merged RSS Feed' }}
          OUTPUT_DESCRIPTION: ${{ vars.OUTPUT_DESCRIPTION || 'Combined RSS feed' }}
          OUTPUT_LINK: ${{ vars.OUTPUT_LINK || 'https://github.com' }}
          MAX_ITEMS: ${{ vars.MAX_ITEMS || '300' }}
          MAX_AGE_DAYS: ${{ vars.MAX_AGE_DAYS || '30' }}
        run: python scripts/merge_rss.py

      - name: Commit and push merged feed
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"
          git add docs/feed.xml
          git diff --staged --quiet || git commit -m "chore: update merged RSS feed [$(date -u '+%Y-%m-%d %H:%M UTC')]"
          git push
